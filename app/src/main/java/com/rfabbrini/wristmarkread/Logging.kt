package com.rfabbrini.wristmarkread

/**
 * Single Logcat tag for the whole app, so a device session can be followed with:
 *
 * ```
 * adb -s <watch> logcat -s WristMarkRead
 * ```
 */
const val LOG_TAG: String = "WristMarkRead"
